@{
    ViewBag.Title = "Home Page";
}
<div class="container">
    <form ng-controller="FormCtrl">
        <div class="row">
            <div class="col-xs-3">
            </div>
            <div class="col-xs-1">
                <select ng-model="method">
                    <option value="GET">GET</option>
                    <option value="HEAD">HEAD</option>
                </select>
            </div>
            <div class="form-group col-xs-3">
                <input type="text" class="form-control" id="urlinput" placeholder="Enter url" ng-model="url">
            </div>
            <div class="col-xs-1">
                <button class="btn" id="fetchbtn" ng-click="MakeRequest()">Fetch</button>
                <button class="btn" id="storebtn">Store</button>
            </div>
        </div>
    </form>
    <div>
        <p id="outputTarget"></p>
    </div>
</div>
